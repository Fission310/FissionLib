buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        "com.android.tools.build:gradle:4.2.2"
    }
}

allprojects {
    repositories {
        mavenCentral()
        google()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
